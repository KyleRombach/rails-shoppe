-if @cart
  %p.navbar-text.navbar-right
    -if @cart.empty?
      Your cart is empty.
    -else
      = link_to cart_path do
        %i.fi-shopping-cart
        My Cart
        (
        %span.cart-count>= @cart.count
        )